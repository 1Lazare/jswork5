let newMessages = true;
let active = true;

if (active == true) {
    console.log (`სალამი შემოგევლე`) ;
    if (newMessages == true) {
        console.log (`შენ გაქვს რამოდენიმე წაუკითხავი მესიჯი`)
    }
}